TECNOLOGIAS
angular y firebase...


CODIGO

para la parte del back y como base de datos utilizamos firebase

en la parte del front con angular, creamos un modulo y un enrutamiento personalizado
para los componentes de la aplicacion, tambien un modelo de datos y un servicio para 
crear los metodos de conexion atravez de angular firestore...
se creo un pipe para facilitar la busqueda de un elemento atravez de un filtro
igualmente tenemos una carpeta llamada shared donde creamos estilos reutilizables para estos 
componentes.

por ultimo dejamos saber tambien el uso de ngModel, validaciones,
tarjetas de notificacion, un spinner loading y el uso de
router y activatedRoute, reutilizacion de componentes etc


WEB

en este crud podemos crear una tabla de colores a travez de un formulario,
tenemos en la aplicacion 2 enlaces de navegacion los cuales se encargan de
redirigirnos a la tabla o al formulario de la misma.

en la parte del formulario encontramos 2 campos para ingresar el color
y el tono del color a los cuales a�adimos una validacion que informa
al usuario que los campos son requeridos lo que impide almacenar en la base de datos
informacion vacia.

una vez creada el color satisfactoriamente veremos en una tabla los datos
ingresados por el usuario

en la tabla encontramos un boton para eliminar y otro para editar
utilizando la opcion de editar vemos como nos redirige al formulario
y a�ade en los campos de este la informaciona a editar y cambia la accion del
formulario de guardar a actualizar

por ultimo vemos como podemos eliminar una targeta tanto de la aplicacion como
de la base de datos

este dise�o cuenta con el ajuste a dispositivos moviles
